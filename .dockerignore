# =============================================================================
# .dockerignore：控制仓库根目录作为 Docker 构建上下文时发送哪些文件
# -----------------------------------------------------------------------------
# Compose 在根目录的 docker/ 中，Go 模块在 backend/ 中，因此 build.context
# 必须是仓库根目录。Dockerfile 只会 COPY backend/go.mod、backend/go.sum、
# backend/cmd 和 backend/internal；这里再排除无关目录，减少传输并避免泄密。
# =============================================================================

# Git、IDE、AI 工具和任务工作区不参与镜像构建。
.git/
.github/
.idea/
.vscode/
.agents/
.claude/
.codex/
.codegraph/
.pi/
.trellis/
.cache/

# 前端由独立 Node.js 工具链构建，当前 Go 后端镜像不需要它。
frontend/

# 本地环境变量、运行数据和缓存不得发送给 Docker。
**/.env
**/.env.*
!**/.env.example
backend/.data/
backend/.cache/
docker/.env

# Go 镜像只需要生产源码，不需要测试、文档和本地产物。
backend/test/
backend/**/*_test.go
backend/*.md
backend/travel-agent
backend/travel-agent.exe
**/*.test
**/coverage.out
**/coverage.html
