spring:
  application:
    name: agent
  # 开启虚拟线程
  threads:
    virtual:
      enabled: true

  datasource:
    url: jdbc:postgresql://localhost:5432/travel_agent
    username: username
    password: yourPassword

  ai:
    vectorstore:
      pgvector:
        schema-name: rag
        table-name: t_knowledge_vector



